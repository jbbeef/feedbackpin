=== FEEDBACKPIN — AGENT PROGRESS LOG ===

Session 1 (Initializer):
- Scaffolded project with Next.js 16.2.2, TypeScript strict mode, Tailwind CSS v4, Supabase, Playwright, Stripe
- Created CLAUDE.md with product overview, tech stack, folder structure, dev commands, architectural decisions, and MVP scope
- Created feature_list.json with 21 MVP features across auth, project creation, screenshot capture, share links, reviewer canvas, comments, dashboard, and payments (all currently failing)
- Created init.sh dev server startup script with port-conflict detection
- Created all placeholder source files:
    src/app/ (layout, page, dashboard, login, projects/new, projects/[id], review/[token])
    src/app/api/ (projects CRUD, comments CRUD, review token endpoint)
    src/components/ (Canvas, CommentPin, CommentSidebar, ProjectCard)
    src/hooks/ (useComments)
    src/lib/supabase/ (client, server, types)
    src/workers/ (screenshot)
    payments/ (stripe)
- Created .env.example with all required environment variable keys
- Created supabase/migrations/001_initial_schema.sql with:
    projects table (id, owner_id, name, type, source_url, screenshot_url, created_at)
    comments table (id, project_id, x_percent, y_percent, body, author_name, resolved, parent_id, created_at)
    share_tokens table (id, project_id, token, created_at)
    RLS policies for owner access on all tables
    Service-role pattern for reviewer (anonymous) writes
- All features currently marked as failing — ready for coding agent

Note: create-next-app@latest installed Next.js 16.2.2 (not 14 as originally specified). Next.js 16 uses App Router and is compatible with the planned architecture. AGENTS.md (auto-generated by Next.js) warns of breaking changes vs training data — coding agents should read node_modules/next/dist/docs/ before writing Next.js code.

Session 2 (2026-04-03):
- Worked on: F001 (sign up), F002 (sign in), F003 (log out) — auth infrastructure
- What was done: Implemented full Supabase Auth flow. Built login/page.tsx with email+password sign-in and sign-up tabs, LogoutButton client component, and protected dashboard/page.tsx with server-side auth guard. Created src/lib/supabase/client.ts (browser client via createBrowserClient), src/lib/supabase/server.ts (server/route handler client via createServerClient with cookie passthrough + service role variant), and src/lib/supabase/types.ts (full Database type for projects/comments/share_tokens). Created src/proxy.ts (Next.js 16 renamed middleware → proxy) for session refresh and route protection redirects.
- Status: PARTIAL — code is complete and compiles; all routes respond correctly (/, /login 200; /dashboard 307→/login without auth). Features F001/F002/F003 cannot be marked passing until Supabase credentials are configured in .env.local and verified end-to-end in a browser.
- Known issues / blockers for next session:
    * .env.local is not set up — next agent must configure NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY before auth can be tested
    * Next.js 16 breaking change: middleware.ts is now proxy.ts and the exported function must be named `proxy` (not `middleware`)
    * Next.js 16 breaking change: params and searchParams in page.tsx are Promises — must be awaited
    * Supabase email confirmation: if enabled in Supabase project settings, sign-up shows a "check your email" screen; if disabled, session is set immediately and user is redirected to /dashboard. Login page handles both cases.

Session 3 (2026-04-03):
- Worked on: F001 (sign up), F002 (sign in), F003 (log out) — verifying auth end-to-end
- What was done: Fixed a Next.js 16 navigation race condition in login/page.tsx and LogoutButton.tsx — router.push()+router.refresh() doesn't reliably propagate the Supabase session cookie to the server before the next request; replaced with window.location.href for a full-page navigation. Installed @playwright/test (separate from the playwright browser package already in package.json), created playwright.config.ts and tests/auth.spec.ts with 6 browser-automation tests covering sign-up form render, invalid password error, sign-in success, wrong password error, logout redirect, and post-logout access guard. All 6 tests pass (12s run). Created a pre-confirmed test user via Supabase admin API (testuser@feedbackpin-e2e.dev) to avoid free-tier email rate limits during test runs.
- Status: PASSING — F001, F002, F003 verified end-to-end via Playwright and marked true in feature_list.json.
- Known issues / TODOs for next session:
    * @playwright/test was installed as a devDependency (not in original CLAUDE.md package list) — needed for test runner separate from playwright browser
    * Supabase free tier limits signup confirmation emails to ~4/hour; tests use admin-created user to bypass
    * Next.js 16 key patterns: proxy.ts (not middleware.ts), params is a Promise in pages/routes, window.location.href for auth navigations
    * Next feature to implement: F004 — owner creates a project by pasting a URL

=== END OF LOG ===

Session 4 (2026-04-03):
Worked on: F004 — Owner can create a project by pasting a URL
What was done: Implemented POST /api/projects (create) and GET /api/projects (list) route handlers with typed responses and auth guards. Built the /projects/new client page with a project-name field, URL/Image/PDF type selector, and URL input; Image/PDF types show a "coming soon" placeholder. Updated the dashboard to fetch projects with comment counts via embedded relation and render ProjectCard components. Fixed the Database type in types.ts to include Relationships: [] on each table (required by postgrest-js v2 GenericTable). Fixed playwright.config.ts to use workers: 1 to prevent session conflicts with the shared test user. Wrote 4 Playwright tests for F004; all 10 tests pass.
Status: PASSING — F004 verified end-to-end via Playwright and marked true in feature_list.json.
Known issues / TODOs for next session:
    * Next feature: F005 (image upload) or F007 (screenshot capture after URL project creation)
    * The dashboard uses as unknown as cast for the comments(count) embedded relation — Supabase JS v2 types don't propagate embedded count queries through our manual types
    * playwright.config.ts set to workers: 1 since all tests share one Supabase test user

=== END OF LOG ===

Session 5 (2026-04-04):
Worked on: F005 — Owner can create a project by uploading an image
What was done: Added image upload support to /projects/new. Created a server-side API route (POST /api/projects/upload) that authenticates the user, validates the file (PNG/JPG/WebP, ≤20 MB), and uploads directly to Supabase Storage via the REST API with the service role key as the Authorization header (bypasses storage RLS). The POST /api/projects route was extended to accept an optional screenshot_url field. Created the project-uploads Supabase Storage bucket via the Storage management API. Added tests/fixtures/test-image.png for Playwright tests. Updated the F004 test to reflect the new Image UI (file input instead of "coming soon" placeholder), and added 4 new Playwright tests for F005. Key insight: @supabase/supabase-js createClient with service_role key still hits RLS on storage.objects; the workaround is to use the Supabase Storage REST API directly with Authorization: Bearer {service_role_key}.
Status: PASSING — F005 verified end-to-end via Playwright (14/14 tests pass) and marked true in feature_list.json.
Known issues / TODOs for next session:
    * Storage RLS policies in 002_storage.sql were not applied (not needed since the upload route uses the service role key directly via REST API)
    * @supabase/supabase-js service role client does not bypass storage RLS in SSR context — direct REST API call is the workaround
    * Next feature: F006 (PDF upload) or F007 (screenshot capture for URL projects) — F007 depends on a Railway worker; F006 can follow same upload pattern

=== END OF LOG ===

Session 6 (2026-04-05):
Worked on: CLAUDE.md architectural update + F007 — Screenshot captured automatically after URL project creation
What was done:
  - Updated CLAUDE.md to document the Supabase Storage RLS workaround: the @supabase/supabase-js service role client still triggers RLS on storage.objects in SSR context; the fix is calling the Supabase Storage REST API directly with Authorization: Bearer {service_role_key}.
  - Implemented src/workers/screenshot.ts: a standalone HTTP server (port 3001) that accepts POST / with { projectId, sourceUrl }, captures a full-page screenshot via Playwright chromium, uploads to Supabase Storage via REST API, and updates projects.screenshot_url via PostgREST. Loads .env.local automatically in non-production. Can be started with: node --experimental-strip-types src/workers/screenshot.ts
  - Added SCREENSHOT_WORKER_URL=http://localhost:3001 to .env.local.
  - Updated playwright.config.ts to start the screenshot worker as a second webServer entry alongside npm run dev; Playwright manages the worker lifecycle during test runs.
  - Implemented GET/PATCH/DELETE /api/projects/[id] route with auth guard and typed responses.
  - Extended POST /api/projects to fire-and-forget the screenshot worker after creating a URL project (uses SCREENSHOT_WORKER_URL env var; silently no-ops if unavailable).
  - Implemented /projects/[id] page: fetches project data, shows a spinner while screenshot_url is null, polls every 2s (up to 60s) until screenshot appears, then renders the image with data-testid="project-screenshot".
  - Added tests/screenshot.spec.ts with 3 Playwright tests for F007. All 17 tests pass (1.5 min run).
Status: PASSING — F007 verified end-to-end via Playwright and marked true in feature_list.json.
Known issues / TODOs for next session:
  - Node.js 24 emits a MODULE_TYPELESS_PACKAGE_JSON warning when running the worker (harmless — can be silenced by adding "type":"module" to package.json, but this would require testing for breakage in other parts of the project).
  - The screenshot worker uses fire-and-forget from the API route: screenshot_url is null on initial project creation and populates asynchronously. The /projects/[id] page polls until it appears.
  - Next feature: F008 (uploaded image displayed as canvas) or F009 (generate shareable review link).

=== END OF LOG ===
