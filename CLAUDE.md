# FeedbackPin — Claude Code Context

## What this product is

FeedbackPin is a visual feedback and annotation tool for designers. Owners paste a live URL or upload a file (image or PDF), share a link, and reviewers click anywhere on the canvas to pin a comment. No reviewer login required. It targets freelance designers and small agencies replacing Markup.io after its 2025 pricing changes.

## Tech Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Framework | Next.js (App Router) | 16.2.2 |
| Language | TypeScript | strict mode |
| Styling | Tailwind CSS | v4 |
| Database / Auth / Realtime / Storage | Supabase | latest |
| Screenshot capture | Playwright | latest (separate Railway worker) |
| Payments | Stripe | latest (Phase 3 placeholder only) |
| Deployment (web) | Vercel | — |
| Deployment (worker) | Railway | — |

> IMPORTANT: Next.js 16 has breaking changes vs earlier versions. Read `node_modules/next/dist/docs/` before writing any Next.js code. See AGENTS.md in the project root.

## Folder Structure

```
feedbackpin/
├── CLAUDE.md                        # This file
├── AGENTS.md                        # AI agent guidance (auto-generated by Next.js)
├── feature_list.json                # MVP feature registry with pass/fail status
├── claude-progress.txt              # Agent session progress log
├── init.sh                          # Dev server startup script
├── .env.example                     # Required environment variables (copy to .env.local)
├── supabase/
│   └── migrations/
│       └── 001_initial_schema.sql   # Postgres schema + RLS policies
├── payments/
│   └── stripe.ts                    # Stripe helpers (Phase 3 placeholder)
└── src/
    ├── app/
    │   ├── layout.tsx               # Root layout
    │   ├── page.tsx                 # Landing / marketing page
    │   ├── dashboard/page.tsx       # Owner project list
    │   ├── login/page.tsx           # Auth page (sign up + sign in)
    │   ├── projects/
    │   │   ├── new/page.tsx         # Create project wizard
    │   │   └── [id]/page.tsx        # Owner project view (canvas + sidebar)
    │   ├── review/[token]/page.tsx  # Public reviewer canvas (no auth)
    │   └── api/
    │       ├── projects/
    │       │   ├── route.ts               # GET list, POST create
    │       │   └── [id]/
    │       │       ├── route.ts           # GET, PATCH, DELETE
    │       │       └── comments/route.ts  # GET, POST comments for project
    │       ├── comments/[id]/route.ts     # PATCH (resolve/reopen), DELETE
    │       └── review/[token]/route.ts    # GET project by share token (public)
    ├── components/
    │   ├── Canvas.tsx               # Interactive annotation canvas
    │   ├── CommentPin.tsx           # Individual pin marker on canvas
    │   ├── CommentSidebar.tsx       # Comment list + thread view
    │   └── ProjectCard.tsx          # Dashboard project card
    ├── hooks/
    │   └── useComments.ts           # Supabase Realtime comment subscription
    ├── lib/
    │   └── supabase/
    │       ├── client.ts            # Browser Supabase client
    │       ├── server.ts            # Server-side Supabase client (SSR)
    │       └── types.ts             # Generated / hand-written DB types
    └── workers/
        └── screenshot.ts            # Playwright screenshot worker (runs on Railway)
```

## How to Run the Dev Server

```bash
# Copy env vars first
cp .env.example .env.local
# Fill in .env.local with real Supabase + Stripe keys

npm run dev
# App available at http://localhost:3000
```

Or use the convenience script:

```bash
bash init.sh
```

## How to Run Playwright Tests

```bash
npx playwright test
```

To run in headed mode for debugging:

```bash
npx playwright test --headed
```

## Key Architectural Decisions

### Pin positions stored as percentages (not pixels)
`x_percent` and `y_percent` (0–100) are stored in the database rather than absolute pixel coordinates. This means pins render correctly regardless of viewport size, zoom level, or if the underlying image is re-screenshotted at a different resolution.

### No reviewer authentication
Reviewers access projects via a UUID-based `share_tokens.token`. This token is checked server-side before any data is returned. No session or cookie is created for reviewers — they are fully anonymous. This is intentional to reduce friction.

### Playwright runs as a separate worker (Railway)
Screenshot capture is CPU/memory-intensive and would time out on Vercel's serverless limits. The `src/workers/screenshot.ts` process runs on Railway and is triggered via an internal API call or queue. It writes the result to Supabase Storage.

### Supabase RLS for multi-tenancy
All data access is controlled by Row Level Security policies. Owners can only see their own projects. Public read of a project is only possible when the request includes a valid share token.

### App Router only
No Pages Router. All routing uses Next.js App Router conventions (`page.tsx`, `layout.tsx`, Route Handlers `route.ts`).

### Supabase Storage uploads must use the REST API directly (not the JS SDK) in server routes
`@supabase/supabase-js` `createClient` with the service role key still triggers "new row violates row-level security policy" on `storage.objects` when called from a Next.js Route Handler, even with `auth: { persistSession: false }`. The root cause is that `@supabase/ssr`'s `createServerClient` propagates the user session from cookies and that session JWT overrides the service role key in the Authorization header sent to the Storage API.

**The fix:** call the Supabase Storage REST API directly via `fetch`:

```ts
const res = await fetch(
  `${process.env.NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/project-uploads/${fileName}`,
  {
    method: "POST",
    headers: {
      Authorization: `Bearer ${process.env.SUPABASE_SERVICE_ROLE_KEY}`,
      "Content-Type": file.type,
    },
    body: buffer,
  }
);
```

This is implemented in `src/app/api/projects/upload/route.ts`. Apply the same pattern for any future server-side storage writes.

## What is Explicitly Out of Scope for MVP

- Team/workspace accounts (single owner per project only)
- Video or audio annotations
- Real-time collaborative cursor tracking
- Custom domains for review links
- Email notifications
- Mobile-native apps
- OAuth / social login (email+password only)
- Stripe billing (Phase 3 — placeholder files exist but no logic)
- Comment editing after submission
- Internationalization (i18n)
