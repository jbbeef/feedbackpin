// TODO: Playwright screenshot worker — runs on Railway, not Vercel
// Accepts { projectId, url } via HTTP, captures full-page screenshot,
// uploads to Supabase Storage, updates projects.screenshot_url
